node_modules
.output
.wrangler
.env
.env.*

# Rehosted Lovable CDN assets — served directly from Hostinger public_html via FTP,
# not bundled into the git-deployed build (repo was too large for Hostinger's git checkout).
public/images/uploads/
public/pdfs/uploads/
public/files/uploads/
